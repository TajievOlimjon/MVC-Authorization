@using WebAvtorize.Areas.Admin.Models.ModelDTO
@model List<CourseDTO>

<div class="row">
    <div class="col-md-12">
        <a asp-action="Create" asp-controller="Course" class="btn btn-success">Добавить</a>
    </div>
</div>
<div class="row">
    <div class="col-md-12">
        <table class="table">
            <thead>
            <tr>     
                    <td>Name</td>
                 
                   
                           
            </tr>
            </thead>
            <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    
                    <td>@item.Name</td>
                   
                   
                   
                   

                    <td>
                        <a asp-action="Edit" asp-controller="Course" asp-route-id="@item.Id">
                            Измениить
                        </a>
                        <a asp-action="Delete" asp-controller="Course" asp-route-id="@item.Id">
                            Удалить
                        </a>
                    </td>
                </tr>
            }
            </tbody>
        </table>
    </div>
</div>