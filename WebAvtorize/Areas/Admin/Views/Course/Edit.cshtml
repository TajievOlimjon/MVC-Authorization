@using WebAvtorize.Areas.Admin.Models.ModelDTO
@model CourseDTO


<h1>Изменить</h1>
<form asp-action="Edit"  method="post">
    <div class="row">
        <div class="col-lg-12">
            <div class="form-group">
                 <label>Названия курса:</label>
                <input asp-for="Name" type="text" class="form-control " placeholder="Введите названию курса" tabindex="1">
            </div>
            
            

           @*  <div class="form-group">
                          <label>Категория:</label>
                 <select class="form-select" asp-for="CtegoryId" asp-items="@(new SelectList(ViewBag.Categories,"Id","Name"))" >
                      <option value="0" selected>Open this select menu</option>
                 </select>

              </div>*@
            
           
            <div class="form-group">
                <div asp-validation-summary="All" class="text-danger"></div>
            </div>
            <div class="card-footer">
                <center>
                    <button type="submit" class="btn btn-primary">Да</button>
                    <a asp-action="Index" asp-controller="Course" class="btn btn-secondary">Неть</a>
                </center>
            </div>
        </div>
    </div>
</form>
